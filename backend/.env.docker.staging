# ===========================================
# CECOR - Staging Environment (Keycloak Local)
# ===========================================

# PostgreSQL Configuration
POSTGRES_HOST=172.19.0.3
POSTGRES_PORT=5432
POSTGRES_USER=cecor
POSTGRES_PASSWORD=cecor123
POSTGRES_DB=cepr_db
POSTGRES_SSLMODE=disable

# Keycloak SSO Configuration (STAGING - lar-sso local)
SSO_CLIENT_ID=cecor-backend
SSO_CLIENT_SECRET=cecor-staging-secret
SSO_REDIRECT_URL=http://localhost:8082/*
# STAGING: Usa Keycloak do lar-sso via host.docker.internal
# O Keycloak do lar-sso expõe a porta 8081 no host
# O backend acessa via host.docker.internal:8081
SSO_AUTH_URL=http://host.docker.internal:8081/realms/lar-sso/protocol/openid-connect/auth
SSO_TOKEN_URL=http://host.docker.internal:8081/realms/lar-sso/protocol/openid-connect/token
SSO_USER_INFO_URL=http://host.docker.internal:8081/realms/lar-sso/protocol/openid-connect/userinfo
OIDC_ISSUER=http://host.docker.internal:8081/realms/lar-sso

# Keycloak Admin API Configuration (STAGING)
KEYCLOAK_ADMIN_URL=http://host.docker.internal:8081
KEYCLOAK_ADMIN_REALM=master
KEYCLOAK_ADMIN_CLIENT_ID=admin-cli
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=admin
KEYCLOAK_TARGET_REALM=lar-sso

# SMTP Configuration (Email) - usar valores do .env local ou deixar vazio para staging
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=dev.david.garcia.alonso@gmail.com
SMTP_PASSWORD=Fusca@75
SMTP_FROM_EMAIL=dev.david.garcia.alonso@gmail.com
SMTP_FROM_NAME=CECOR - Staging
FRONTEND_URL=http://localhost:4201
SUPPORT_EMAIL=suporte@cecor.org
